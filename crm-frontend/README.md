
# CRM Frontend (React + Vite)

## Setup
1. Copy `.env.example` to `.env` and set `VITE_API_URL=http://localhost:5000/api`
2. Install deps: `npm install`
3. Run: `npm run dev` (site will be at http://localhost:3000)
